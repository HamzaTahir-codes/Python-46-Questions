def reversal(str_reverse):
    return str_reverse[::-1]


test = "I am Hamza!"
print(reversal(test))


# TODO: FEEDBACK: we should not use 'string' as parameter we have python's string module
#  which will be override if use string as parameter (import string)
